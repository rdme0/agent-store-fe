import { PagePlaceholder } from './PagePlaceholder'

export function RunsPage() {
  return (
    <PagePlaceholder
      eyebrow="Runs"
      title="Execution history"
      description="Run status, output, and payment details will be available here in a later slice."
    />
  )
}
