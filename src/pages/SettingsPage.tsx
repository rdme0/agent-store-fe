import { PagePlaceholder } from './PagePlaceholder'

export function SettingsPage() {
  return (
    <PagePlaceholder
      eyebrow="Settings"
      title="Workspace settings"
      description="Workspace configuration will live here. No server state is read by this foundation shell."
    />
  )
}
