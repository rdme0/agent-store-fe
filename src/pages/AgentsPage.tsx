import { PagePlaceholder } from './PagePlaceholder'

export function AgentsPage() {
  return (
    <PagePlaceholder
      eyebrow="Agents"
      title="Your agent catalog"
      description="Agent discovery and management will appear here once the API contract is connected."
    />
  )
}
